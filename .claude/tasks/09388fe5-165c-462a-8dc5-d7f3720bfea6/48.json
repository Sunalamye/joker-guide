{
  "id": "48",
  "subject": "2.5 實現 JokerDef::compute() 方法",
  "description": "實現通用計算引擎：\n\n```rust\nimpl JokerDef {\n    pub fn compute(&self, ctx: &ScoringContext, state: &JokerState) -> JokerBonus {\n        match &self.effect {\n            EffectDef::Fixed { chips, mult, x_mult, money } => { ... }\n            EffectDef::CountBonus { filter, per_card } => { ... }\n            EffectDef::Conditional { condition, bonus } => { ... }\n            EffectDef::Stateful { calculator } => { ... }\n            EffectDef::Retrigger { filter, count } => { ... }\n            EffectDef::RuleModifier => JokerBonus::default(),\n        }\n    }\n}\n```\n\n**驗證**：單元測試每種效果類型\n\n[完成後 super-analyst-pro 完整分析當前的修改 如果沒有問題就 commit 後 執行下一個 todo 直到所有都完成才結束 ]",
  "activeForm": "實現 compute() 方法",
  "status": "pending",
  "blocks": [
    "49"
  ],
  "blockedBy": [
    "47"
  ]
}
